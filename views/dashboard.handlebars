<div>
    <div>
        <div>
            <input type="text">
            <button id="search" class="btn btn-primary">Search</button>
        </div>

        <div>
            Add movie
        </div>

    </div>
    {{#if movies.length}}
    <div class="container">
        {{#each movies as |movie|}}
        {{> movies movie}}
    </div>
    {{/if}}
</div>